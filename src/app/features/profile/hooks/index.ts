// This file exports profile-related hooks
// When hooks are created, they should be exported from here

// Example placeholder hook for now
export const useProfile = () => {
  return {
    isLoading: false,
    error: null,
    profile: null,
  };
};

// Add more hook exports as they are implemented 